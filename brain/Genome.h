#pragma once

#include <vector>

#include "Gene.h"

// Genome is the roadmap to creating the entity. Entity is full of sensors, brain, and actions
// Sensors, actions should remain constant (maybe we can fiddle with that later)
// but neurons should be in flux

class Genome
{
public:
	
};